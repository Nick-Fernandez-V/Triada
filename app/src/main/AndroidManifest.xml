<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.Nick.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.Triadas">


        <!-- android:theme="@style/Theme.AppCompat.NoActionBar" -->
        <activity
            android:name=".principales.SplashActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".adapters.FullScreenImagenActivity"/>
        <activity android:name=".registrar.Registro1Activity" />
        <activity android:name=".registrar.Registro2Activity" />
        <activity android:name=".registrar.Registro3Activity" />
        <activity android:name=".registrar.Registro4Activity" />
        <activity android:name=".registrar.SeleccionPaisActivity" />
        <activity android:name=".principales.MenuApp" /> <!-- android:label="@string/title_activity_menu_app" -->
        <activity
            android:name=".principales.LoginActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>